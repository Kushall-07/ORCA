# ============================================================
# ORCA backend runtime dependencies
# UTF-8. Phase 1 (infrastructure / data foundation).
# LangGraph + Groq are added in Phase 5, GIS/routing libs in Phase 2.
# ============================================================

# ---- Web framework ----
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.4
anyio==4.15.0
h11==0.16.0
click==8.5.0
idna==3.19

# ---- Configuration / validation ----
pydantic==2.13.5
pydantic-settings==2.15.0
pydantic_core==2.46.5
annotated-types==0.8.0
annotated-doc==0.0.5
typing_extensions==4.16.0
typing-inspection==0.4.4
python-dotenv==1.2.3

# ---- HTTP client (health checks now; Open-Meteo in Phase 4) ----
httpx>=0.27,<1.0

# ---- Datastores ----
sqlalchemy[asyncio]>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
geoalchemy2>=0.15,<1.0
redis>=5.0,<6.0

# ---- Serialization ----
pyyaml>=6.0,<7.0

# ---- Deterministic core: GIS + numerics (Phase 2) ----
# Used only for offline geometry / grid math. No network access.
numpy>=1.26,<3.0
shapely>=2.0,<3.0
pyproj>=3.6,<4.0

# ---- Static GIS ingestion scripts only (Phase 4, scripts/) ----
# Pure-Python readers; NOT imported by the app runtime.
pyshp>=2.3,<4.0
tifffile>=2024.1,<2027.0

# ---- Phase 5: agentic orchestration + LLM ----
# LangGraph orchestrates the pipeline; Groq is the ONLY LLM provider.
# The deterministic core (risk / policy / decision / routing / reasoning) does
# NOT import any of these - see tests/test_phase5_invariants.py.
langgraph>=1.0,<2.0
langchain-core>=1.0,<2.0
groq>=1.0,<2.0
